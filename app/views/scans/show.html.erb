<h1>Scan</h1>
<p>
  <b>Name:</b>
  <%=h @scan.name %>
</p>

<h2>Listing Hosts</h2>
<%= render :partial => 'hosts/list', :locals => { :hosts => @scan.hosts } %>


<%= link_to 'Edit', edit_scan_path(@scan) %> |
<%= link_to 'Back', scans_path %>
